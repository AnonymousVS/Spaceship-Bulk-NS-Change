# ============================================================
# Spaceship Bulk NS Change - Public Config
# ============================================================
# ⚠️ API Key/Secret อยู่ใน private repo: AnonymousVS/config/spaceship-api.conf
# ไฟล์นี้เก็บเฉพาะ settings ที่ไม่ใช่ความลับ
# ============================================================

# --- Nameservers ที่ต้องการเปลี่ยนไป ---
NS1="aria.ns.cloudflare.com"
NS2="bob.ns.cloudflare.com"

# --- Delay ระหว่างแต่ละโดเมน (วินาที) ---
DELAY=2

# --- Telegram แจ้งเตือน (optional - ไม่ใส่ = ไม่แจ้ง) ---
TG_BOT_TOKEN=""
TG_CHAT_ID=""
